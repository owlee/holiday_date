<%= form_for(@holiday_date) do |f| %>
  <% if @holiday_date.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@holiday_date.errors.count, "error") %> prohibited this holiday_date from being saved:</h2>

      <ul>
      <% @holiday_date.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :holiday_id %><br>
    <%= f.text_field :holiday_id %>
  </div>
  <div class="field">
    <%= f.label :calendar_date_id %><br>
    <%= f.text_field :calendar_date_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
